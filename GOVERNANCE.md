# Governance

Shiplet Runtime Kit is maintained in the open. Small fixes can be merged by
maintainers after review. Contract changes require a documented proposal,
backwards-compatibility notes, and at least one implementation example.

The project follows a lightweight maintainer model. Decisions are recorded in
issues or pull requests so users and provider authors can follow the reasoning.
